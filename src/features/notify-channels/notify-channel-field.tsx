// Implementation moved to shared/ui/domain/labeled-field so shared/ui/domain/**
// can use it (that layer may not import from features/**).
export { LabeledField as NotifyChannelField } from "@/shared/ui/domain/labeled-field";
